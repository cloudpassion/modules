#from aiogram_dialog import DialogRegistry

# from .settings import register_settings_dialog


def register_dialogs(dp):
    pass
    #registry = DialogRegistry(dp)
    #register_settings_dialog(dp=dp, registry=registry)
