CHOST="x86_64-pc-linux-gnu"
CBUILD="x86_64-pc-linux-gnu"

# all pc
source all.conf

# targets
source targets.conf

LUA_SINGLE_TARGET="${LUA_SINGLE_TARGET_ALL}"
LUA_TARGETS="${LUA_TARGETS_ALL}"
PHP_TARGETS="${PHP_TARGETS_ALL}"

PYTHON_TARGETS="${PYTHON_TARGETS_ALL}"
PYTHON_SINGLE_TARGET="${PYTHON_SINGLE_TARGET_ALL}"
USE_PYTHON="${USE_PYTHON_ALL}"
PYTHON_ABIS="${PYTHON_ABIS_ALL}"
RUBY_TARGETS="${RUBY_TARGETS_ALL}"

LLVM_TARGETS="${LLVM_TARGETS_ALL}"

INPUT_DEVICES="${INPUT_DEVICES_ALL}"
VIDEO_CARDS="${VIDEO_CARDS_ALL} nvidia i915 intel"

UQD_USE1="X minimal alsa pulseaudio nvenc xinerama"
UQD_USE2="vaapi vdpau vulkan bluetooth dbus wayland"
UQD_DB_USE="" #postgres sqlite odbc freetds mysql oci8"

UQD_EXCLUDE1="-systemd"
UQD_EXCLUDE2=""
UQD_EXCLUDE3=""

UQD_EXCLUDE="${UQD_EXCLUDE1} ${UQD_EXCLUDE2} ${UQD_EXCLUDE3}"

UQD_USE="${UQD_EXCLUDE} ${UQD_USE1} ${UQD_USE2} ${UQD_DB_USE}"


USE="${ALL_USE} ${UQD_USE}"

EMERGE_DEFAULT_OPTS="${EMERGE_DEFAULT_OPTS_ALL}"

FEATURES="${ALL_FEATURES} getbinpkg"
